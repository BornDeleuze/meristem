<div {{did-insert (fn this.setupSwiper)}}>
  <div class="swiper">
    <div class="swiper-wrapper">
      {{#each @images as |image|}}
        <div class="swiper-slide">
          <img src="{{image.src}}"/>
        </div>
      {{/each}}
    </div>
  </div>
</div>